module.exports = {
  initialiseSetTax: function (arg0_user) {
    //Convert from parameters

    //Separate class dependent taxes and general taxes after initial prompt
  },

  setTax: function (arg0_user, arg1_tax_type, arg2_amount) {
    //Convert from parameters
    //Update UI
  }
};
