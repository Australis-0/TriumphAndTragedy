module.exports = {
  mine: function (arg0_user, arg1_actions) {
    //Convert from parameters
    var user_id = arg0_user;
    var actions = Math.ceil(arg1_actions);

    //Declare local instance variables
    var actual_id = main.global.user_map[user_id];
  }
};
